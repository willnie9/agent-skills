{
  "$schema": "http://json-schema.org/draft-07/schema#",
  "$id": "https://internal/skills/_shared/module-code.schema.json",
  "title": "MODULE_CODE 权限编码",
  "description": "项目通用 MODULE_CODE 枚举常量,frontend-page-design 注册路由/菜单时用。新模块未分配前用 WORKBENCH 兜底 + TODO(perm) 注释。",
  "type": "string",
  "pattern": "^[A-Z][A-Z0-9_]*$",
  "examples": [
    "WORKBENCH",
    "MODULE_A_VIEWABLE",
    "MODULE_A_MANAGE",
    "MODULE_B_LIST",
    "SYSTEM_MANAGE"
  ],
  "placeholder_policy": {
    "default_fallback": "WORKBENCH",
    "required_comment_pattern": "TODO\\(perm\\):",
    "find_command": "grep -rn 'TODO(perm)' src/"
  }
}

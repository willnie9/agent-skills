# Agent Skill Pipeline - 全量源码文件树与详细说明

（*注：为了清晰展现核心逻辑，本文件树已排除 `node_modules` 依赖库、`output` / `logs` / `runtime` 等自动生成的临时产物文件，仅展示所有**源代码和配置文件***）

### 根目录共享配置与全局文档
```text
.claude/skills/
├── docs/                                 # 【归档】非核心的系统管理与架构演进文档
│   ├── GLOSSARY.md                       # 术语表，统一定义领域名词
│   ├── DESIGN-DECISIONS.md               # 设计决策文档，架构设计思路与 trade-off 分析
│   ├── STATUS.md                         # 状态同步文件，记录各个 skill 的开发和升级进度
│   └── WORKFLOW.md                       # 全局工作流说明文档
├── project.config.json                   # 【核心配置】跨项目通用的配置文件（定义了路由、视图、HTTP库等路径）
└── schemas/
    └── project-config.schema.json        # 针对 project.config.json 的 JSON Schema 语法约束校验文件
```

---

## 1. module-flow (总线编排器)
**职责**：全流程调度器，负责接管用户的原始输入，并决定调用以下哪个子技能。
```text
module-flow/
├── SKILL.md                              # 【核心指令】大模型执行此技能的行为准则与决策树
├── schemas/
│   └── task-input.schema.json            # 规范大模型解析用户意图后生成的标准任务数据结构
├── scripts/
│   └── check-resume-state.mjs            # 【脚本】用于在执行中断后，检查上一步状态并恢复执行的 Node.js 脚本
└── references/                           # 【参考文档库】
    ├── cancel-and-resume.md              # 说明如何处理“用户取消操作”及“恢复操作”的规则
    ├── common-pitfalls.md                # 易错点备忘录（防止大模型踩坑）
    ├── data-strategy-options.md          # 场景2中的数据策略选项（Mock数据还是静态数据）说明
    ├── error-recovery.md                 # 遇到编译错误或解析错误时的自动恢复策略
    ├── stage-contracts.md                # 定义各阶段（A/B/C/D）之间的输入输出契约格式
    └── task-template.md                  # 内部任务模板定义
```

---

## 2. master-go-to-code (设计稿转代码)
**职责**：拉取 MasterGo 设计稿，切分为 DOM 树结构并提取图片/SVG 资源。
```text
master-go-to-code/
├── SKILL.md                              # 【核心指令】大模型解析设计稿的行为准则
├── package.json                          # Node.js 依赖配置 (例如 playwright 等)
├── package-lock.json                     # 依赖版本锁定文件
├── schemas/                              
│   ├── dom-tree-v1.schema.json           # DOM 树 JSON 结构校验文件
│   └── svg-paths.schema.json             # 提取出来的 SVG 路径数据结构校验
├── scripts/                              # 【核心执行脚本库】
│   ├── compare-pixel.mjs                 # 像素级 UI 差异对比脚本
│   ├── compare-tokens.mjs                # 设计 Token 差异对比脚本
│   ├── extract-image-names.mjs           # 从设计稿数据中提取图片名称
│   ├── fetch-and-parse.mjs               # 核心：调用 MasterGo API 获取源数据并初步解析
│   ├── iterate.mjs                       # 组装切割的 JSON 块，并生成预览 HTML 的迭代脚本
│   ├── merge-refined.py                  # Python脚本：合并经过细化的 JSON 数据块
│   ├── refine-chunks.py                  # Python脚本：细化并清洗局部数据块
│   ├── refine-dom-tree.py                # Python脚本：全局梳理 DOM 树的嵌套和绝对定位
│   ├── render.mjs                        # 将 DOM 树渲染成可供浏览器预览的真实代码
│   ├── seed-test-data.mjs                # 自动填充测试用 Mock 数据的脚本
│   ├── split-dsl.py                      # Python脚本：由于画板太大，将原始 DSL 切割为小块
│   └── validate-dom-tree.mjs             # 验证最终生成的 DOM 树是否合法的脚本
└── references/                           # 【参考文档库】
    ├── common-pitfalls.md                # 提取 DOM 时的易错点
    ├── dom-tree-spec.md                  # DOM 树自定义规范文档
    ├── dsl-css-mapping.md                # MasterGo 属性到 CSS 样式的映射规则
    ├── dsl-issues.md                     # 已知的设计稿 DSL 解析疑难杂症处理方案
    ├── dsl-rules.json                    # DSL 属性解析映射字典
    ├── imgdir-mapping.md                 # 图片目录存储规范
    └── svg-to-png.md                     # SVG 转换为 PNG 图片的规则
```

---

## 3. yapi-to-code (接口转 TypeScript)
**职责**：读取 YApi 接口，推导出严谨的 Typescript `interface`、`enum` 及 Mock 代码。
```text
yapi-to-code/
├── SKILL.md                              # 【核心指令】接口对齐和推导规则（拒绝any、处理枚举等）
├── scripts/
│   └── validate-define.mjs               # 【脚本】校验大模型生成的 define.ts 语法和规范是否正确
└── references/
    ├── api-templates.md                  # 最终要生成的 API 文件和 Mock 文件的固定代码模板
    └── common-pitfalls.md                # 接口推导时的易错点（如嵌套对象忘记展开）
```

---

## 4. frontend-page-design (前端页面组装器)
**职责**：读取 `dom-tree.json` 和 `api.ts`，组装成完整的 Vue 单文件组件并注册路由。
```text
frontend-page-design/
└── SKILL.md                              # 【核心指令】Vue 3 页面组装、BEM规范、菜单挂载等指令大全
    (注: 该目录下还包含如 references/code-style.md 等具体代码风格文件，受限于列表未全部穷举，均受 SKILL.md 统筹)
```

---

## 5. auto-ui-explorer (全模块 E2E 测试编排)
**职责**：全模块扫描 → 生成 UI 词典 → baseline 持久化 → 增量回归测试，支持跨页流程编排。
```text
auto-ui-explorer/
├── SKILL.md                              # 【核心指令】E2E 测试编排的行为准则与增量执行策略
├── config/
│   └── auto-ui-explorer.config.json       # 噪音黑名单 + 点击事件模式 + 表单组件类型
├── schemas/
│   ├── ui-dictionary.schema.json          # Step 1 UI 词典输出的 JSON Schema
│   ├── baseline.schema.json              # baseline 持久化结构校验
│   └── experience.schema.json            # experience 经验复用结构校验
├── scripts/
│   ├── analyze-module.mjs                # 全目录递归扫盘器（读 config）
│   ├── validate-dictionary.mjs           # Step 1 产物校验
│   ├── validate-flow-plan.mjs            # Step 2.6 SP 引用/覆盖率/空值拦截
│   └── diff-baseline.mjs                # Step 0.7 增量判断（对比 baseline.json）
└── references/
    ├── common-pitfalls.md                # E2E 测试的易错点
    ├── execution-strategies.md           # 增强执行步骤（搜索/分页/CRUD）
    ├── baseline-strategy.md              # baseline 持久化与经验复用策略
    └── noise-filter-rules.md             # 噪音元素过滤规则
```

---

## 6. playwright-skill (MCP 浏览器适配层)
**职责**：封装基于 MCP 的无头浏览器操作，帮助大模型点击页面和验收。
```text
playwright-skill/
├── SKILL.md                              # 【核心指令】大模型操作浏览器时的“动作指导书”
└── config/                               # 【配置文件库】
    ├── credentials.local.example.json    # 账号密码配置文件示例
    ├── credentials.local.json            # 实际存放账号密码的文件（不会提交到 Git）
    ├── playwright-skill.config.example.json # 目标域名、登录页面 Selector 的配置示例
    ├── playwright-skill.config.json      # 实际运行时的测试配置文件
    └── playwright-skill.config.schema.json  # 针对测试配置文件的结构约束 Schema
```

---

## 7. yunxiao-bug-fix (阿里云效修虫工具)
**职责**：连接阿里云效，实现 Bug 读取、代码分析定位、修复和留言闭环。
```text
yunxiao-bug-fix/
├── SKILL.md                              # 【核心指令】读取缺陷、源码定位和修复的全生命周期指导
└── config/
    ├── yunxiao-comment.md                # 修完 Bug 后自动在云效中回复的 Markdown 留言模板
    └── yunxiao-comment.schema.json       # 留言模板的数据结构校验
```

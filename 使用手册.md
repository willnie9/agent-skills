# 使用手册 · 触发方式与行为契约

这套 skill 通过自然语言触发。下面 6 种是最常用的触发场景，每一种列出：触发条件、示例话术、实际行为、产出物。

读完之后你就知道哪些场景能用、哪些场景跑不出预期，避免把它当万能工具。

---

## 场景 1：全流程自动生产线（Auto E2E）

**触发条件**

- 输入同时包含：MasterGo URL、YApi 项目信息（项目 ID 或接口链接）
- 输入含 auto 关键词之一：`auto / yolo / 全跑 / 一气呵成 / 别问 / 自动`
- 没有指定“xxx 模块”（说明是新建，不是改老的）

**示例话术**

> 帮我建一个【客户列表】模块，设计稿链接是 `https://mastergo.com/...`，YApi 项目 ID 是 `28`。走 auto 模式直接跑全流程。

**实际行为**

调用 `module-flow` 进入场景 1，依次执行：

1. Stage A `master-go-to-code`：拉 DSL → dom-tree.json + svg-paths.json + 图片资源
2. Stage B `yapi-to-code`：从 YApi 模糊搜索匹配接口 → 生成 define.ts + api.ts
3. Stage C `frontend-page-design`：组装 Vue SFC，注册路由和菜单
4. Stage D `playwright-skill`：启动浏览器跑到新页面截图

**产出物**

- `<imgDir>` 下的图片和 SVG
- `<module>/define.ts`、`<module>/api.ts`
- `<module>/Index.vue` + 路由 + 菜单注册
- 截图（落到 playwright-skill 的 runtime 目录）

**不会做的事**

- 不会回写后端代码
- 不会自动提交 git
- A/B/C 任一阶段 schema 校验失败 → 立即熔断

---

## 场景 2：纯前端页面（带 mock 数据）

**触发条件**

- 输入只有 MasterGo URL，没有 YApi 信息
- auto 模式默认走数据策略 A（全 mock）；非 auto 模式会问你选 A（mock）还是 D（静态）

**示例话术**

> 这个设计稿 `https://mastergo.com/...` 帮我建个新页面，没有 API，数据策略选 A。

**实际行为**

走 module-flow 场景 2，跳过 Stage B，Stage C 在生成 Vue 时直接写入符合 `_shared/schemas/common-response.schema.json` 格式的 mock 数据。

**产出物**

- 同场景 1，但没有 define.ts/api.ts，Vue 文件内嵌 mock

**不会做的事**

- 不会替你接真实接口
- 不会生成 MSW / 全局 mock 服务（mock 是组件级写死的）

---

## 场景 3：老模块增量 / 迭代 / 重构

**触发条件**

- 输入含 MasterGo URL
- 输入显式包含模块名 + 操作词之一：`xxx 模块 增量 / 迭代 / 重构`
- 模块名必须用户显式说，不允许 AI 推断

**示例话术**

> 针对现有的【设备管理】模块做一下增量，加上这个链接里的筛选项 UI：`https://mastergo.com/...`

**实际行为**

进入 module-flow 场景 3/4/5。**必跑 Step 0.5 e2e 定位**：先用 Playwright 打开站点，确认你说的模块在哪个路由、对应哪个 `Index.vue`。

之后行为按场景区分：

| | 增量（3） | 迭代（4） | 重构（5） |
|---|---|---|---|
| 建新文件 | 加新子组件 | ❌ 精准 Edit 现有文件 | ❌ 替换 Index.vue 内容 |
| 路由 / 菜单 | 不动 | 不动 | 不动 |

**产出物**

- 增量：新子组件 + 现有 Index.vue 增加 import
- 迭代：现有 Vue 文件被局部修改
- 重构：Index.vue 内容被替换，路由菜单保留

**不会做的事**

- 不会自己猜模块名（猜错会改错文件）
- 不会跳过 Step 0.5（跳过等于盲改）

---

## 场景 4：纯接口对接（不生成 UI）

**触发条件**

- 输入只有 YApi URL 或项目 ID + 接口关键词
- 没有 MasterGo URL

**示例话术**

> 帮我把这个 Yapi 接口转成代码：`http://yapi/...`
>
> YApi 项目 ID 是 15，帮我生成一下里面关于【退款】的接口。

**实际行为**

不走 module-flow，直接调 `yapi-to-code`。推导 Interface、生成枚举常量，落盘到 `project.config.json` 配置的 API 目录。

**产出物**

- `define.ts`：类型定义
- `api.ts`：请求函数
- 不使用 `any`（如果 YApi 定义不清，会落 issue 提示你确认）

**不会做的事**

- 不会建 Vue 文件
- 不会动路由

---

## 场景 5：Playwright 自动化测试

**触发条件**

- 输入含明确的浏览器操作意图：`登录 / 点击 / 填写 / 截图 / 跑一下`
- 提到具体页面或操作流程

**示例话术**

> 用 playwright 帮我登录系统，去【水表列表】点一下新增按钮，填个假数据，截个图发给我确认下没报错。

**实际行为**

直接调 `playwright-skill`。读取 skill 自身的 config（账号、URL），打开浏览器执行操作步骤，全程截图。

**产出物**

- 截图序列（落到 runtime 目录）
- 如果中途报错，附错误堆栈和最后一帧截图

**不会做的事**

- 不会改业务代码
- 不会写持久化的 e2e 测试用例（这是冒烟，不是回归测试）

---

## 场景 6：云效 Bug 自动修复

**触发条件**

- 输入含云效缺陷链接 `https://devops.aliyun.com/proj/bug/...`
- 或输入含云效工单号 + 修复意图

**示例话术**

> 帮我看看这个云效 Bug 是怎么回事：`https://devops.aliyun.com/proj/bug/...`，顺便修一下。

**实际行为**

调 `yunxiao-bug-fix`，按 SOP 依次：

1. 拉缺陷详情和截图附件
2. grep 源码定位疑似组件
3. （可选）调 Playwright 复现
4. 给修复方案 + 执行修改
5. 在云效工单下留言记录修复过程

**产出物**

- 代码修改
- 云效工单评论

**不会做的事**

- 不会自动 commit / push
- 不会自动改工单状态（评论而已，状态留给用户决定）

---

## auto 模式的边界

凡是话术里出现 `auto / yolo / 全跑 / 一气呵成 / 别问 / 自动` 的，都进入无确认模式。这种模式下：

- harness 层会拦截非白名单的 AskUserQuestion
- 字段缺失走自动决策表（见 module-flow/SKILL.md）
- 模块名仍然必须显式说，**不会** auto 推断

如果 auto 模式跑出意料之外的结果，先看 `.claude/state/auto-mode.flag` 是否被正确清理。

---

## 触发不出来怎么办

skill 是“关键词匹配 + frontmatter description”触发的。如果你的话术没命中：

- 显式指名 skill：`走 module-flow 场景 1` / `调 yapi-to-code`
- 或者贴 `PROJECT-TASK` 块（格式见 module-flow/references/）
- 或者把关键词加进话术：MasterGo URL → Stage A 必触发；YApi 项目 ID → Stage B 必触发

不要在一句话里同时让我做多个不相关的任务。一次跑一个完整链路，干净。

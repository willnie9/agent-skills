# Agent Skill Pipeline - 使用手册

您可以随时用自然语言唤起我，触发不同的工作流。以下是 7 种最常见的触发方式及使用示例：

## 模式 1：一键全自动生产线 (Auto E2E)
**适用场景**：拿到设计稿和接口，需要从 0 到 1 生成完整模块，不想一步步确认。
**触发话术**：
> "帮我建一个【客户列表】模块，设计稿链接是 `https://mastergo.com/...`，YApi 项目 ID 是 `28`。走 auto 模式直接跑全流程。"

**我的行为**：
我会调用 `module-flow`，自动识别 MasterGo 的内容，自动从 Yapi 搜索对应接口并生成 TS 代码，自动为您生成 `Index.vue` 并注册路由和菜单。最后，我会启动内置的浏览器，去新跑起来的页面帮您截图验收。

## 模式 2：纯前端页面开发 (带 mock 数据)
**适用场景**：后端接口还没出，但您想先把前端页面和 Mock 跑起来。
**触发话术**：
> "这个设计稿 `https://mastergo.com/...` 帮我建个新页面，没有 API，数据策略选 A (全 mock)。"

**我的行为**：
我会切图并解析结构，生成 Vue 文件，并自动在里面写满合规的 Mock 静态数据，让您能直接在本地看到带假数据的界面。

## 模式 3：老模块增量与重构
**适用场景**：不新建模块，只是在现有的代码里追加东西或重写。
**触发话术**：
> "针对现有的【设备管理】模块做一下增量，加上这个链接里的筛选项UI：`https://mastergo.com/...`"

**我的行为**：
我不会新建文件夹，而是会**先打开浏览器**验证您说的老模块到底在哪个路径下，找到对应的 `Index.vue` 后，只针对性地把新设计稿的部分写成新的子组件合并进去。

## 模式 4：纯 API 接口对接
**适用场景**：UI 您想自己手敲，只要我帮您处理烦人的 TypeScript 类型体操。
**触发话术**：
> "帮我把这个 Yapi 接口转成代码：`http://yapi...`" 或 "YApi 项目ID 是 15，帮我生成一下里面关于【退款】的接口。"

**我的行为**：
我只触发 `yapi-to-code`，帮您推导最严谨的 Interface，生成枚举常量字典，绝对不会使用 `any`，直接落盘到您配置好的 API 文件夹里。

## 模式 5：功能自动化测试 (QA 模式)
**适用场景**：改了底层逻辑，需要我当测试员帮您点一点看看坏没坏。
**触发话术**：
> "用 playwright 帮我登录系统，去【水表列表】点一下新增按钮，填个假数据，截个图发给我确认下没报错。"

**我的行为**：
我只触发 `playwright-skill`，纯后台读取配置为您模拟点击、填写表单，最后把截图通过对话界面发给您。

## 模式 6：全模块 E2E 回归测试
**适用场景**：改了底层公共组件或路由逻辑，需要验证多个模块是否受到影响。
**触发话术**：
> "帮我把【客户管理】模块下的所有页面跑一遍 E2E 测试，看看有没有坏掉的地方。"

**我的行为**：
我触发 `auto-ui-explorer`，先扫描整个模块的目录结构，生成 UI 词典（页面列表 + 交互元素）。然后对比上次的 `baseline.json`，只对新增或修改的页面做全量扫描，未改动的页面跳过。最后通过 MCP 浏览器逐页执行测试，截图汇报结果。如果是第一次跑，会建立完整的 baseline 供下次增量对比。

## 模式 7：云效 Bug 自动修复
**适用场景**：接到了阿里云效的缺陷工单。
**触发话术**：
> "帮我看看这个云效 Bug 是怎么回事：`https://devops.aliyun.com/proj/bug/...`，顺便修一下。"

**我的行为**：
我只触发 `yunxiao-bug-fix`，读取缺陷详情，去您的源码里用 grep 定位出问题的组件，给出修复方案并执行修改，并在云效工单下帮您留言。
